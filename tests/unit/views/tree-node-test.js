import { moduleFor, test } from 'ember-qunit';

moduleFor('view:tree-node', 'Unit | View | tree node');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
